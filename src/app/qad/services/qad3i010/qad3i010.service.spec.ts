/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Qad3i010Service } from './qad3i010.service';

describe('Qad3i010Service', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Qad3i010Service]
    });
  });

  it('should ...', inject([Qad3i010Service], (service: Qad3i010Service) => {
    expect(service).toBeTruthy();
  }));
});
