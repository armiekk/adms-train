/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { QaActivityLevelDirective } from './qa-activity-level.directive';

describe('Directive: QaActivityLevel', () => {
  it('should create an instance', () => {
    let directive = new QaActivityLevelDirective();
    expect(directive).toBeTruthy();
  });
});
